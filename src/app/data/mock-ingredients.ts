import { Ingredient } from '../models/ingredient.model'

export const mockIngredients: Ingredient[] = [
  new Ingredient({
    name: 'Apples',
    amount: 5,
  }),
  new Ingredient({
    name: 'Tomatoes',
    amount: 10,
  }),
]
